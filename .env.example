# For local development (without Docker), uncomment the line below:
# REACT_APP_API_URL=http://localhost:5000/api

# When running with Docker, leave REACT_APP_API_URL commented out.
# The Nginx reverse proxy handles routing /api to the backend automatically.
